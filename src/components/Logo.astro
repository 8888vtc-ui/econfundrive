---
// Logo luxe ECOFUNDRIVE - SVG professionnel
---

<div class="logo-luxe">
  <div class="logo-icon">
    <svg width="60" height="60" viewBox="0 0 60 60" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true" role="img">
      <defs>
        <linearGradient id="goldGradient" x1="0%" y1="0%" x2="100%" y2="100%">
          <stop offset="0%" style="stop-color:#d4af37;stop-opacity:1" />
          <stop offset="50%" style="stop-color:#f4e4bc;stop-opacity:1" />
          <stop offset="100%" style="stop-color:#b8941f;stop-opacity:1" />
        </linearGradient>
        <filter id="glow">
          <feGaussianBlur stdDeviation="3" result="coloredBlur"/>
          <feMerge>
            <feMergeNode in="coloredBlur"/>
            <feMergeNode in="SourceGraphic"/>
          </feMerge>
        </filter>
      </defs>
      <!-- Fond avec coins arrondis -->
      <rect width="60" height="60" rx="12" fill="url(#goldGradient)"/>
      <!-- Symbole voiture/chauffeur stylisé -->
      <path d="M15 25 L20 20 L25 18 L35 18 L40 20 L45 25 L45 35 L40 38 L35 40 L25 40 L20 38 L15 35 Z" 
            fill="#0a0a0a" opacity="0.9"/>
      <!-- Détails premium -->
      <circle cx="30" cy="28" r="3" fill="#d4af37"/>
      <path d="M22 30 L30 27 L38 30" stroke="#d4af37" stroke-width="2" fill="none" stroke-linecap="round"/>
      <path d="M20 32 L25 30 L35 30 L40 32" stroke="#d4af37" stroke-width="1.5" fill="none" stroke-linecap="round"/>
    </svg>
  </div>
  <div class="logo-text">
    <div class="logo-title">ECOFUNDRIVE</div>
    <div class="logo-subtitle">Chauffeur Privé VTC</div>
  </div>
</div>

<style>
  .logo-luxe {
    display: flex;
    align-items: center;
    gap: 1rem;
  }

  .logo-icon {
    flex-shrink: 0;
    filter: drop-shadow(0 0 15px rgba(212, 175, 55, 0.6));
    transition: transform 0.3s ease;
  }

  .logo-icon:hover {
    transform: scale(1.05);
  }

  .logo-title {
    font-size: 1.5rem;
    font-weight: 700;
    background: linear-gradient(135deg, #d4af37, #f4e4bc);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    letter-spacing: 2px;
    text-transform: uppercase;
    line-height: 1.2;
  }

  .logo-subtitle {
    font-size: 0.75rem;
    color: var(--luxe-gray, #e5e7eb);
    letter-spacing: 1px;
    font-weight: 500;
    margin-top: 2px;
  }
</style>
